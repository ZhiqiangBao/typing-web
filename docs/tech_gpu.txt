A graphics processing unit contains thousands of small cores designed to run many threads in parallel. Unlike a central processor optimized for low-latency serial work, a GPU hides memory latency by switching between huge numbers of threads. This makes it ideal for data-parallel problems such as matrix multiplication, image processing, and deep learning, where the same operation is applied across millions of elements at once.
